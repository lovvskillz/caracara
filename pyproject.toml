[tool.poetry]
name = "caracara"
version = "0.1.0"
description = ""
authors = ["vadim <vadim@minehub.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
Django = "^4.1"
django-environ = "^0.9.0"
djangorestframework = "^3.13.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
